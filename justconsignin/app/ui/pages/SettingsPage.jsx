import {ArrowLeft,CheckCircle2} from 'lucide-react';
export default function SettingsPage({consignors,items,onBack}){const connected=items.filter(i=>i.shopifyProductId).length;return <div className="jci-page"><div className="jci-pagehead"><div><button className="jci-link" onClick={onBack}><ArrowLeft size={16}/> Back</button><p className="jci-eyebrow" style={{marginTop:10}}>Configuration</p><h1>Settings</h1><p className="jci-sub">Reference information for the Shopify-connected workspace. No new settings backend has been introduced.</p></div></div><div className="jci-cardgrid"><section className="jci-card"><h2>Shopify connection</h2><p className="jci-sub"><CheckCircle2 size={15} style={{verticalAlign:'middle'}}/> Running through the existing authenticated Shopify app.</p><div className="jci-statrow"><div><span>Consignors</span><strong>{consignors.length}</strong></div><div><span>Shopify products</span><strong>{connected}</strong></div></div></section><section className="jci-card"><h2>Consignment metadata</h2><p className="jci-sub">Existing received date remains in place. Consignment term and expiry date are additive fields on the existing consignment item Metaobject.</p></section><section className="jci-card"><h2>Publishing</h2><p className="jci-sub">POS and Online Store publishing remain controlled per item inside the existing Shopify product workflow.</p></section></div></div>}
